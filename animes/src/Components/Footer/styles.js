import styled from 'styled-components';

export const FooterWrapper = styled.footer`
  background-color: #282c34;
  color: white;
  padding: 20px;
  text-align: center;
  position: relative;
  z-index: 1; /* Garante que o footer fique acima do conteúdo */
`;
